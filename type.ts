import { IconType } from 'react-icons';

export interface ServiceInterface {
  title: string;
  about: string;
  Icon: IconType;
}

export interface SkillInterface {
  name: string;
  level: string;
  Icon: IconType;
}
